<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Essence of Music</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css" integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css" integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
</head>
<body>

  <div class="jumbotron jumbotron-fluid">
      <div class="container-fluid">
        <h1 class="display-4 text-center">Essence Of Music</h1>
        <nav>
          <a href="/">Home</a>
          {{#if loggedIn}}
          <button id="logout" class="btn-no-style">Logout</button>
          {{else}}
          <a href="/login">Login</a>
          {{/if}}
        </nav>
        
      </div>
  </div>
  <main>
    <div class="container-fluid">
      <div class="row">
        <h2>What's your Vibe?</h2>
      </div>
      <div class="gallery row">
        {{{body}}}
      </div>
    {{#if loggedIn}}

    <script src="/js/logout.js"></script>
    <button id="logout" class="btn-no-style">Logout</button>
    {{else}}
    <a href="/login">Login</a>

    {{/if}}
      <form id='form'>
        <div class="form-group row justify-content-center mt-5 ml-5">
          <label for="colFormLabelLg" class="col-sm-10 col-md-3 col-form-label col-form-label-lg ">Search By Artist</label>
          <div class="col-sm-10 col-md-5">
            <input type="text" class=" form-control form-control-lg" id="artist" placeholder="Artist's name">
          </div>
          <div class="col col-sm-6 col-md-4 my-1">
            <button type="submit" id='submission' class="btn btn-primary ">Submit</button>
          </div>
        </div>

      </form>
    </div>
  </main>
  <script src="/js/search-artist.js"></script>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>
</html>