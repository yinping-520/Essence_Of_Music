<section class="container">
  <h3 class="genre">genre</h3>
  <!-- Without the custom helper, the entire timestamp is printed -->
  {{!-- <h4>Opening Hour: {{genre.starting_date}}</h4>
  <!-- Using a custom helper 'format_time', the time has been formatted -->
  <h4>Closing Hour: {{format_date genre.ending_date}}</h4>
  <section class="flex-row justify-center"> --}}
    {{!-- {{#each genre.paintings as |painting|}}
    {{> painting-details}}
    {{/each}} --}}
  </section>
</section>
